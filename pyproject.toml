[project]
name = "fraud-openenv"
version = "0.1.0"
description = "Fraud detection OpenEnv project"
requires-python = ">=3.10"

dependencies = [
    "openai",
    "openenv>=0.2.0"
]

[project.scripts]
server = "inference:run"